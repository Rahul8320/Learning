<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <% if (user) { %>
      <a href="/dashboard" class="navbar-brand"><i class="fa fa-graduation-cap"></i>
        <span class="ms-1">Result Management System</span>
      </a>
      <% }else { %>
        <a href="/" class="navbar-brand"><i class="fa fa-graduation-cap"></i>
          <span class="ms-1">Result Management System</span>
        </a>
        <% } %>

          <button class="navbar-toggler" data-bs-target="#my-nav" data-bs-toggle="collapse" aria-controls="my-nav"
            aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div id="my-nav" class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
              <li class="nav-item active">
                <% if (user ) { %>
                  <a href="/dashboard" class="nav-link"><i class="fa fa-home me-1"></i>Home</a>
                  <% } else{ %>
                    <a href="/" class="nav-link"><i class="fa fa-home me-1"></i>Home</a>
                    <% } %>
              </li>
              <% if (user) { %>
                <li class="nav-item active">
                  <a href="/add" class="nav-link"><i class="fa fa-user-plus me-1"></i>Add Student</a>
                </li>
                <% } %>


                  <li class="nav-item active">
                    <a href="mailto:rahuldey832000@gmail.com" class="nav-link"><i
                        class="fa fa-envelope me-1"></i>Contact</a>
                  </li>
                  <% if (user) { %>
                    <div class="dropdown">
                      <button class="btn btn-dark dropdown-toggle text-white
          type=" button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if (user.profile_pic) { %>
                         <img src="/uploads/<%= user.profile_pic %>"  width="36" class="rounded-3 me-1">
                        <% } else { %> 
                          <i class="fa fa-user-circle me-1"></i>
                          <% } %> 
                          Hello, <%= user.name %>
                      </button>
                      <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                        <li><a class="dropdown-item" href="/teacher/profile"> <i class="fa fa-user-circle me-1"></i> My
                            Profile</a></li>
                        
                        <li>
                          <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="/auth/logout"><i class="fa fa-power-off me-1"></i> Logout</a>
                        </li>
                      </ul>

                      <% } %>
            </ul>

          </div>
  </div>
  </div>
</nav>